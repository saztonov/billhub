import type { ThemeConfig } from 'antd'

/** Конфигурация темы Ant Design для BillHub */
export const theme: ThemeConfig = {
  token: {
    colorPrimary: '#1677ff',
    borderRadius: 6,
    fontSize: 14,
  },
}
