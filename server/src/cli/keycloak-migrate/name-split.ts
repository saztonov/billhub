/**
 * Ф3 — split `full_name` → `firstName`/`lastName`. Реэкспорт общей реализации
 * `services/auth/keycloak/name.ts` (одна утилита на Ф2-провижининг и Ф3-импорт).
 */
export { splitFullName } from '../../services/auth/keycloak/name.js';
