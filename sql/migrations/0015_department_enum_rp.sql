-- Миграция 0015: новое значение 'rp' в department_enum — департамент этапа согласования «РП».
--
-- Значение используется ТОЛЬКО в approval_decisions.department_id (этап 3, миграция 0016);
-- пользователям (users.department_id) не назначается — гейт в zod-схеме user и UI-селектах.
--
-- Отдельный файл обязателен: новое значение enum нельзя ИСПОЛЬЗОВАТЬ в той же транзакции,
-- где оно добавлено, а runner оборачивает каждую миграцию в собственную транзакцию (ADR-0002).
-- Использование значения начинается со следующей миграции (0016).

ALTER TYPE public.department_enum ADD VALUE IF NOT EXISTS 'rp';
