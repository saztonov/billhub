-- Миграция 0013: дата отправки распределительного письма (РП).
--
-- Контекст:
--   В реестре РП колонка «Дата» становится двухстрочной «Дата созд. / Отправки».
--   Дата отправки заполняется вручную из реестра (до заполнения — пусто, прочерк).
--   Хранится только в BillHub, в PayHub не уходит.
--
-- Без top-level BEGIN/COMMIT — runner оборачивает миграцию в транзакцию (ADR-0002).
-- Идемпотентность — через IF NOT EXISTS.

ALTER TABLE public.rp_letters
  ADD COLUMN IF NOT EXISTS sent_date date;
