-- Миграция 0011: номер счёта на распределительном письме (РП).
--
-- Контекст:
--   В форме создания РП добавлено поле «Номер счёта» (ручной ввод). Значение хранится
--   только в BillHub (в PayHub не уходит, номер письма/рег.номер PayHub не затрагивает)
--   и выводится в реестре РП справа от суммы.
--
-- Без top-level BEGIN/COMMIT — runner оборачивает миграцию в транзакцию (ADR-0002).
-- Идемпотентность — через IF NOT EXISTS.

ALTER TABLE public.rp_letters
  ADD COLUMN IF NOT EXISTS invoice_number text;
